# controls-primitives

`controls-primitives` is the embedded runtime-primitives layer of krig.
It intentionally stays as one module: these types are small, KMP-friendly building
blocks that are reused by runtime, tests, simulations, and local deployments.
